# Downloaded Linux kernel source (too large)
/linux-*/
/linux/
*.tar.xz
*.tar.gz
*.tar.bz2

# Build artifacts
*.o
*.elf
*.bin
*.a
*.so
build/

# VMM executables
kvm-vmm

# Local Linux boot artifacts (do not commit)
bzImage
initrd.img
initramfs.cpio*

# Guest binaries (no extension)
guest/minimal
guest/hello
guest/counter
guest/multiplication
guest/multiplication_short
guest/fibonacci
guest/hctest
guest/matrix
guest/hello_64
guest/vgademo
guest/fault64

# 1K OS kernel binary
os-1k/kernel

# IDE/Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Kernel build artifacts
.config.old
.tmp_versions/

# Initramfs build artifacts (keep source, ignore build)
initramfs/*.o
initramfs/init

# Python
__pycache__/
*.pyc

# Editor/IDE metadata
.obsidian/
.playwright-mcp/

# Example kernel build artifacts
examples/**/*.elf
examples/**/*.o

# Third-party kernels fetched by tools/third-party.sh
third-party/

# Linux boot artifacts for the smoke tests (see tools/smoke.sh)
initramfs.cpio
